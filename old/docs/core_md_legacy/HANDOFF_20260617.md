# TES_511_Balloon Handoff, 2026-06-17

Purpose: this file is the re-entry note for a new chat or a context-reset
session. Read it before continuing manuscript work. It summarizes the current
authority chain, paper-facing language constraints, validated side branches,
open risks, and the dirty worktree state.

## First Files To Read

Read these in this order before making new claims or edits:

1. `core_md/HANDOFF_20260617.md` (this file).
2. `core_md/README.md`, especially `Current Authority`.
3. `core_md/workflow.md`, especially the top authority rules and current
   exact-position rebuild commands.
4. `core_md/Project_Memory.md`, but treat older sections as historical unless
   they explicitly say current v3p5 authority. Some early lines are stale
   provenance; the later 2026-06-12/16 exact-position paragraphs are the useful
   authority memory.
5. `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_en.tex` and
   `balloon511_nima_draft_zh.tex`.
6. `core_md/balloon511_nima_latex_drafts/paper_resource/manuscript_review_20260617.md`.
7. `core_md/balloon511_nima_latex_drafts/paper_resource/claude_r4_recheck_20260617.md`.

There is no repo-local `AGENTS.md` in `TES_511_Balloon`; only unrelated
`AGENTS.md` files were found outside this repo.

## Current Primary Authority

The current paper-facing rate-level authority is:

`fullstat_v2_exactpos_m50000_s260613`

This is the transported M=50000 sampled production-position delayed-source
chain. It replaces the older axisymmetric/radial-profile delayed-source
compression for headline rate claims.

Current CsI hard-window headline values:

- Step05 day-15 selected background/signal:
  `0.06298036183985109 / 0.0011811656293957314 s^-1`.
- Step06 mission-mean background/signal:
  `0.0631923 / 0.00117281 s^-1` (rounded in docs).
- Step08 cumulative significance:
  `Z20d = 6.130394687582996`.
- Step08 3-sigma time:
  `T3 = 4.777656378990109 d`.
- Step08 20-d 3-sigma flux:
  `4.893649027323553e-05 ph cm^-2 s^-1`.
- Step08 1 Ms 3-sigma flux:
  `6.35099e-05 ph cm^-2 s^-1` (from current docs).
- Optical authority:
  `stepwise_maintenance/step04_opticsim/optics_aeff_authority_f10m_a1.json`,
  with `Aeff(511) = 20.08476 cm2`.

Primary evidence files:

- Step05:
  `stepwise_maintenance/step05_veto_time_axis/outputs_v3p5_centerfinger_fullstat_v2_exactpos_m50000_s260613_l1/step05_v3p5_centerfinger_l1_response_summary.json`
- Step06:
  `stepwise_maintenance/step06_mission_time_variation/outputs_v3p5_centerfinger_fullstat_v2_exactpos_m50000_s260613/`
- Step08:
  `stepwise_maintenance/step08_significance/outputs_v3p5_centerfinger_fullstat_v2_exactpos_m50000_s260613/step08_v3p5_centerfinger_time_dependent_summary.json`
- Convergence:
  `outputs/reports/v3p5_exactpos_convergence_20260614/v3p5_exactpos_convergence_summary.json`
- M-sampling audit:
  `outputs/reports/m_sampling_audit_20260616/m_sampling_audit_summary.json`

## Authority Hierarchy And Do-Not-Misquote Rules

- `fullstat_v2_exactpos_m50000_s260613` is the current CsI primary
  selected-rate authority.
- `fullstat_v2_exactpos` without the M50000 suffix is the M=5000 convergence
  point, not the current headline.
- `fullstat_v2` is the conservative radial-profile baseline cross-check, not
  the current headline.
- Old DEMO2/equiv2602/CeBr3/mainline numbers are legacy provenance under the
  2026-06-12 review hold. Do not quote them as current sensitivity.
- The hard-window selected-rate result is the primary result. Spatial rows
  (`spot_r90`, annular/fixed-template likelihood) are diagnostics/boundary
  checks until a publication-grade spatial-spectral nuisance-profile likelihood
  exists.
- Do not present ROI/`spot_r90` as the prompt-suppression strategy. The
  2026-06-17/18 prompt-entry audit shows old `new_geo_re` had lower
  hard-window prompt because its corresponding side region was blocked by a
  solid side-shield/material column and because source-surface normalization
  differed; the current fix direction is local side-port/side-wall geometry
  closure, not promoting an ROI sidecar.
- The 45 degree line-of-sight atmospheric rows are boundary calculations. The
  reference atmospheric transmission used in the headline is a baseline
  pointing normalization, not a universal balloon pointing or Galactic-center
  trajectory model.
- BGO is a material-control branch, not a replacement authority.
- Upstream optics self-background is a boundary/side branch, not included in
  the primary background budget.

## M-Sampling And Exact-Position Closure

Current convergence status:

- `PASS_EXACTPOS_TRANSPORT_CONVERGENCE`
- recommendation:
  `PROMOTE_EXACTPOS_TO_CURRENT_RATE_AUTHORITY`
- Transport-backed cases:
  M=5000 seeds `260613/260614`, M=20000 seed `260613`,
  M=50000 seed `260613`.
- Total selected line-window background relative range:
  `0.011191491816874802`.
- `Z20d` relative range:
  `0.005508440853127455`.
- Delayed subcomponent relative range:
  `0.18741272004229206`.

Interpretation:

The M/seed convergence claim is only for the total selected hard-window
background and significance, not for the delayed subcomponent as an isolated
plateau. The delayed component fluctuates visibly, but it is only about 6.2% of
the selected hard-window background in the current chain, so the total rate and
significance are stable.

Current M=50000 source-card audit:

- status: `PASS_M_SAMPLING_AUDIT`
- parsed point-source blocks: `50000`
- manifest activity: `85.63669586527698 Bq`
- manifest flux relative delta: `0.0`
- coordinate back-match fraction: `1.0`
- unsampled-nuclide activity: `0.05745012620487438 Bq`
- unsampled fraction: `0.0006708587437242382`

## Delayed Distribution-Invariance Smoke Test

User asked whether the mission-time delayed-activation approximation could be
smoke-tested. That is now done.

Script:

`core_md/balloon511_nima_latex_drafts/paper_resource/build_delayed_distribution_invariance_smoke.py`

Outputs:

- `core_md/balloon511_nima_latex_drafts/paper_resource/delayed_distribution_invariance_smoke_20260617.json`
- `core_md/balloon511_nima_latex_drafts/paper_resource/delayed_distribution_invariance_smoke_20260617.md`
- `core_md/balloon511_nima_latex_drafts/paper_resource/delayed_distribution_invariance_smoke_metrics_20260617.csv`
- `core_md/balloon511_nima_latex_drafts/paper_resource/delayed_distribution_invariance_smoke_parma_rows_20260617.csv`

Status:

`PASS_PARTICLE_FAMILY_REWEIGHTING_SMOKE`

Key values:

- max activity TV after PARMA particle-family reweighting:
  `0.005720326789023363`.
- max PARMA particle-family TV:
  `0.0006764453219244723`.
- max PARMA particle-energy-bin TV:
  `0.009045787776627297`.
- matched ground-state activity:
  `85.02854205952535 Bq`.
- unmatched activity:
  `0.608153806746535 Bq`.
- matched fraction:
  `0.9928984438202029`.

Scope boundary:

This smoke supports the scalar delayed-production/activity fold only at the
particle-family reweighting layer for the present small trajectory range. It
does not prove full physical invariance of isotope/material/position
distributions under altitude, energy, angle, or geomagnetic changes. Stronger
proof would require dedicated low-altitude, reference, and high-altitude
Geant4/MEGAlib activation-production transports using generated PARMA source
cards.

Manuscript status:

- English paragraph inserted in
  `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_en.tex` around
  the mission-time fold section.
- Chinese paragraph inserted in
  `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_zh.tex` around
  the same section.
- PDF text extraction verified the paragraph is rendered in both PDFs.

## BGO Material-Control Branch

Current BGO branch is `Bgo_sample/`, not `../new_geo_re_2`.

Current comparison report:

`outputs/reports/bgo_sample_csi_comparison_20260615/bgo_vs_csi_summary.json`

Status:

`PASS_BGO_SAMPLE_VS_CSI_EXACTPOS_M50000_COMPARISON`

Claim level:

`BGO_SAMPLE_MATERIAL_COMPARISON_AFTER_STEP08_NO_SPATIAL_LIKELIHOOD`

Important boundary:

This is a material-design-threshold control, not a same-threshold veto scan.
CsI uses `50 keV`; BGO uses `70 keV`.

Current BGO/CsI ratios relative to the CsI M=50000 authority:

- mission-mean line-window background ratio: `0.9151907771428296`
  (`-8.4809%`).
- `Z20d` ratio: `1.0496465886293964` (`+4.9647%`).
- 20-d flux-threshold ratio: `0.9527016148414069` (`-4.7298%`).

Paper wording:

Do not write "matched same-threshold comparison". Write "material-control
comparison using material-specific active-veto thresholds". The current paper
also states that a quantitative BGO flight shield claim would require
scintillation/light-collection/electronics/threshold response modeling.

## Upstream Optics Hardware Boundary

Branch:

`stepwise_maintenance/step11_upstream_optics_background/`

Current status:

`DELAYED_GE_PROXY_SELECTED_RATE_CLOSED_PROMPT_SELF_BACKGROUND_OPEN`

What it means:

- Equal-volume/equal-mass active-Ge proxy for the upstream Laue crystal mass.
- Full-particle prompt and activation-production transports were run for the
  proxy with the same eight EXPACS/PARMA particle families.
- Delayed Ge-proxy side component is isolated and replayed through the current
  detector-level selection.

Current delayed Ge-proxy result:

- isolated activity: `0.42567438 Bq`
- isotope records: Ga-70 and Ga-73
- delayed replay triggers: `20000`
- selected line-window events: `0`
- 95% zero-count upper rate:
  `1.2534045848433615e-4 s^-1`
- about `0.20%` of the current primary hard-window background.

Boundary:

This only closes the isolated delayed active-Ge proxy side component. Prompt
self-background from upstream hardware is not in the primary background budget
because the combined prompt simulation needs provenance isolation or
subtraction from ordinary detector/cryostat prompt background. Explicit lens
tile/support hardware is still not modeled.

The `scaled_production_yield = 0.25` factor remains a bookkeeping-provenance
item for this zero-selected-event side component. It is not a current headline
rate driver.

## Paper-Facing Language Constraints

Avoid internal project labels in manuscript prose:

- Do not introduce `v3p5`, `center-finger`, `FREF`, `HEADLINE`, `EVENTLIST`,
  literal `W2`, `Step04`, `Step07`, or similar internal audit labels into
  reader-facing text.
- Use `line-window`, `sampled production-position`, `recorded production
  positions`, `detector-coupled focused photons`, and `mission-time fold`.
- The TeX macro `\wii` currently renders as `W_{511}`; avoid literal `W2` in
  rendered manuscript text.
- "Exact-position" is defined in the manuscript as a method term, but abstract
  and high-level prose should prefer "sampled/recorded production positions".

Do not overclaim:

- Retained reconstruction-reject events are a baseline acceptance choice, not a
  conservative sensitivity lower bound.
- The source-normalization atmospheric transmission is a baseline pointing
  normalization, not a universal balloon or Galactic-center slant-column value.
- M=50000 supports the current final rate/significance claim; it does not prove
  the delayed subcomponent alone has strictly plateaued.
- The delayed distribution-invariance smoke supports particle-family
  reweighting only, not full energy/angle/position invariance.
- BGO control is material/deposited-energy threshold control only.
- Upstream optics Ge-proxy delayed result is a boundary side component only.
- Tabulated flux thresholds are statistical selected-rate thresholds for the
  baseline model; they do not include a systematic uncertainty envelope.

## Current Manuscript And PDFs

Main files:

- `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_en.tex`
- `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_zh.tex`
- `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_en.pdf`
- `core_md/balloon511_nima_latex_drafts/balloon511_nima_draft_zh.pdf`

Latest compile after the delayed distribution-invariance smoke paragraph:

- English PDF: `2993683` bytes, 40 pages, timestamp 2026-06-17 10:46 +0800.
- Chinese PDF: `3335490` bytes, 38 pages, timestamp 2026-06-17 10:46 +0800.

Verification done:

- `python3 -m py_compile
  core_md/balloon511_nima_latex_drafts/paper_resource/build_delayed_distribution_invariance_smoke.py`
- `python3
  core_md/balloon511_nima_latex_drafts/paper_resource/build_delayed_distribution_invariance_smoke.py`
  returned `PASS_PARTICLE_FAMILY_REWEIGHTING_SMOKE`.
- `latexmk -g -xelatex -interaction=nonstopmode -halt-on-error
  balloon511_nima_draft_en.tex`
- `latexmk -g -xelatex -interaction=nonstopmode -halt-on-error
  balloon511_nima_draft_zh.tex`
- TeX log scan found no fatal LaTeX error, undefined control sequence,
  undefined citation, undefined reference, or rerun warning.
- `pdftotext` confirmed the new smoke-test paragraphs are rendered in both
  PDFs.
- `git diff --check` was clean.

Compilation note:

The environment lacked `elsarticle.cls`. `texlive-publishers` could not be
installed without root. A CTAN `elsarticle.zip` was downloaded to `/tmp`, the
class was generated from `elsarticle.ins`, and the generated
`core_md/balloon511_nima_latex_drafts/elsarticle.cls` was placed locally for
compilation. Decide later whether to track this local class file or require a
system TeX package in documentation.

## Current Open Items

Scientific / engineering validation:

- Revan/Mimrec or equivalent reconstruction cross-check for the retained
  multi-hit reconstruction-reject subset.
- Source-specific atmospheric transmission for Galactic-center or other
  low-elevation targets, using a trajectory/source-zenith-angle mission model.
- Dedicated low/reference/high-altitude Geant4 activation-production transports
  if the delayed distribution-invariance approximation must become a stronger
  physical claim.
- Prompt self-background provenance isolation/subtraction for upstream optics
  hardware, plus explicit lens tile/support hardware.
- BGO same-threshold scan only if the paper wants a same-threshold veto claim.
- BGO scintillation/light-collection/SiPM/electronics/threshold-response model
  before any flight-shield performance claim.
- Systematic uncertainty envelope for atmospheric flux normalization,
  activation physics/cross sections, geometry/material tolerances, detector
  response, veto thresholds, and reconstruction efficiency.

Manuscript/submission:

- Final author list, affiliations, CRediT, acknowledgments, competing-interest
  statement, data/archive identifiers, and author-team AI-use policy.
- Systematic bibliography/retraction checks beyond the current first pass.
- Remaining layout warnings are mostly table overfull/underfull boxes. They are
  not current physics blockers.

Documentation/governance:

- Active core docs still contain dangling/stale paths. Clean separately.
- Worktree has many modified/untracked paper-facing artifacts. Commit should be
  done intentionally after selecting scope.

## Git State Warning

As of this handoff, the worktree is dirty. Do not revert unrelated changes.
Many changes predate this handoff and may be user/agent work. Use:

```bash
git status --short
```

Important untracked or newly generated artifacts include:

- `core_md/HANDOFF_20260617.md` (this file).
- `core_md/balloon511_nima_latex_drafts/elsarticle.cls`.
- `core_md/balloon511_nima_latex_drafts/paper_resource/build_delayed_distribution_invariance_smoke.py`.
- `core_md/balloon511_nima_latex_drafts/paper_resource/delayed_distribution_invariance_smoke_20260617.*`.
- `core_md/balloon511_nima_latex_drafts/paper_resource/claude_r4_recheck_20260617.md`.
- `core_md/balloon511_nima_latex_drafts/paper_resource/manuscript_review_20260617.md`.
- `core_md/balloon511_nima_latex_drafts/paper_resource/reference_verification_20260617.md`.
- selection/time-axis figure resources under
  `core_md/balloon511_nima_latex_drafts/paper_source_figure_table/`.

Modified tracked files include, among others:

- `core_md/README.md`
- `core_md/Project_Memory.md`
- `core_md/workflow.md`
- English/Chinese manuscript TeX and PDFs.
- paper-resource tables/figures/review notes.
- BGO sample reports and generation scripts.
- exact-position convergence and M-sampling audit outputs.

If asked to commit, first show a scoped `git status --short` and group changes
by role:

1. manuscript and paper resources;
2. current authority/audit outputs;
3. BGO material-control branch;
4. helper scripts;
5. generated local compile dependency (`elsarticle.cls`) decision.

## Suggested Next Session Start

For a new conversation, say:

> Read `core_md/HANDOFF_20260617.md`, then `core_md/README.md` and
> `core_md/workflow.md`. Continue the NIMA manuscript without quoting legacy
> DEMO2/mainline numbers as current authority.

Then run:

```bash
git status --short
rg -n "TODO|OPEN|Remaining Open Items|same-threshold|Revan|Mimrec|source-zenith|0.25|systematic" \
  core_md/HANDOFF_20260617.md \
  core_md/balloon511_nima_latex_drafts/paper_resource/manuscript_review_20260617.md \
  core_md/balloon511_nima_latex_drafts/paper_resource/claude_r4_recheck_20260617.md
```

## Short Bottom Line

The core rate story is closed at the current selected-rate level:

`M=50000 sampled-position CsI chain -> B/S = 0.0629804 / 0.00118117 cps -> Z20d = 6.13039 -> F3(20d) = 4.89365e-5 ph cm^-2 s^-1`.

The paper should keep saying this is a detector-coupled statistical selected
rate result with explicit boundaries, not a final flight CAD/systematics
performance claim.
